# PWA auto update final fix

Non-admin users may not have access to hard refresh or Force Service Worker Update controls. This patch automatically checks for service worker updates for all users so they receive the latest PWA push fixes.
