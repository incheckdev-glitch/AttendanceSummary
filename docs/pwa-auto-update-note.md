# PWA auto update for all users

This branch adds an automatic service worker update check for every user, not only admins. It prevents non-admin users from being stuck on old PWA JavaScript when they do not have access to the admin-only Force Service Worker Update button.
